# Tasks

- [x] Task 1: 缩小 AI 消息气泡段落间距
  - 修改 `AiAssistant.vue` 中 `.markdown-body` 的 `line-height` 从 `1.6` 降为 `1.45`
  - 修改 `.markdown-body :deep(p)` 的 `margin` 从 `0 0 6px 0` 降为 `0 0 2px 0`
  - 同步调整 `.streaming-text` 的 `line-height` 保持一致

# Task Dependencies
- 无依赖，独立修改
